name: "Chromatic"
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: yarn
    - uses: chromaui/action@v1
      with: 
        projectToken: 'qmxn6x1uac'
        token: ${{ secrets.GITHUB_TOKEN }}